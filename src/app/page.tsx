import CreatorWorkspace from '@/components/CreatorWorkspace';

export default function Home() {
  return (
    <main style={{ minHeight: '100vh', backgroundColor: '#fafafa', padding: '40px 0' }}>
      <CreatorWorkspace />
    </main>
  );
}
